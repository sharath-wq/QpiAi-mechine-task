export default function AdminPage() {
    return (
        <div>
            <h1>Admin Page</h1>
            <p>Welcome to the admin page. Only users with admin privileges can access this page.</p>
        </div>
    );
}